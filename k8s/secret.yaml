apiVersion: v1
kind: Secret
metadata:
  name: trading-wizard-secret
  namespace: trading-wizard
  labels:
    app.kubernetes.io/name: trading-wizard
type: Opaque
stringData:
  # IMPORTANT: Replace this with a secure random key in production
  # Generate with: openssl rand -hex 32
  JWT_SECRET_KEY: "REPLACE_WITH_SECURE_SECRET_KEY"
